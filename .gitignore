node_modules/
renderer/dist/
dist/
out/
release/

# Local override for pointing a dev build at a different Firebase project.
# The shipped default (config/firebase.config.default.json) IS committed — a
# downloaded build has to work without the user configuring anything, and a
# Firebase client config is public by design.
config/firebase.config.json

*.log
.DS_Store
